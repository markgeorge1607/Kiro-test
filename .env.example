# Translation API endpoint (optional — defaults to DeepL free API)
VITE_TRANSLATION_API_ENDPOINT=https://api-free.deepl.com/v2/translate

# Translation API key (optional — enables runtime translation provider)
# When not set, only static locale bundles are used
VITE_TRANSLATION_API_KEY=your-api-key-here
